{
  "name": "poor-richard",
  "version": "0.0.1",
  "description": "Static website for Spotlight PA",
  "repository": "git@github.com:spotlightpa/poor-richard.git",
  "author": "Spotlight PA",
  "license": "MIT",
  "scripts": {
    "format": "yarn run format:eslint; yarn run format:prettier",
    "format:eslint": "eslint --fix --ext .js,.vue --ignore-path .gitignore .",
    "format:prettier": "prettier --ignore-path .gitignore --write **/**/*.{scss,js,yml,vue}",
    "build:go": "./build-go.sh",
    "build:parcel": "parcel build src/entrypoints/* --no-autoinstall --experimental-scope-hoisting",
    "build:stage": "yarn run build:go && yarn run build:parcel && hugo version && hugo --minify --environment staging --baseURL ${DEPLOY_PRIME_URL:-https://www.spotlightpa.org}",
    "build:prod": "yarn run build:go && yarn run build:parcel && hugo version && hugo",
    "start:api": "go run ./cmd/spotlightpa-api",
    "start:noreload": "yarn start:api & parcel src/entrypoints/* & hugo server --disableLiveReload & wait",
    "start:full": "yarn start:api & parcel src/entrypoints/* & hugo server --disableFastRender & wait",
    "start:frontend": "parcel src/entrypoints/* & hugo server & wait",
    "start": "yarn start:api & parcel src/entrypoints/* & hugo server & wait",
    "test": "yarn run test:eslint && yarn run test:prettier",
    "test:eslint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "test:prettier": "prettier --ignore-path .gitignore --check **/**/*.{scss,js,yml,vue}"
  },
  "dependencies": {
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "@fortawesome/fontawesome-free": "^5.12.1",
    "@fortawesome/fontawesome-svg-core": "^1.2.27",
    "@fortawesome/free-regular-svg-icons": "^5.12.1",
    "@fortawesome/free-solid-svg-icons": "^5.12.1",
    "@fortawesome/vue-fontawesome": "^0.1.10",
    "@fullhuman/postcss-purgecss": "^2.3.0",
    "@newswire/frames": "^0.3.1",
    "@vue/component-compiler-utils": "^3.0.0",
    "alpinejs": "^2.4.0",
    "autoprefixer": "^9.5.1",
    "bulma": "^0.9.0",
    "journalize": "^2.4.0",
    "parcel-bundler": "^1.12.4",
    "postcss-cli": "^7.1.0",
    "vue": "^2.6.10",
    "vue-template-compiler": "^2.6.10"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.2.0",
    "eslint-config-prettier": "^6.10.0",
    "eslint-config-standard": "14.1.1",
    "eslint-plugin-import": ">=2.21.2",
    "eslint-plugin-node": ">=7.0.0",
    "eslint-plugin-prettier": "^3.1.4",
    "eslint-plugin-promise": ">=4.0.1",
    "eslint-plugin-standard": ">=4.0.0",
    "eslint-plugin-vue": "6.2.2",
    "prettier": "^2.0.1",
    "vue-hot-reload-api": "^2.3.4"
  }
}
